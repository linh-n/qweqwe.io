version: "3"
services:
  web:
    container_name: react-prod
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt/live/qweqwe.io
    restart: unless-stopped
